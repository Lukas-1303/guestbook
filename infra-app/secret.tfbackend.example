# 주의: 이 파일의 이름을 'secret.tfbackend'로 변경하고, 본인의 S3 버킷 이름을 입력하세요.
# 'secret.tfbackend'는 .gitignore에 등록되어 있어 깃허브에 업로드되지 않으므로 안전합니다.
# 실행 명령어: terraform init -backend-config="secret.tfbackend"
bucket = "본인이-만든-고유한-s3-버킷-이름-입력"
